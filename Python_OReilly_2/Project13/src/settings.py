from datetime import datetime

# The recipients list is a list of tuples in the (name, email) format
RECIPIENTS = [('paulo', 'paulo@me.com'),
              ('pat', 'pat@me.com'),
              ('cubs', 'cubs@champs.com')]

STARTTIME = datetime(2015, 4, 7)
DAYCOUNT = 10 # will not skip weekends